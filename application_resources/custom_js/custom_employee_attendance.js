//add year datepicker
$("#datepicker").datepicker( {
    format: "mm-yyyy",
    viewMode: "year", 
    minViewMode: "year"
});

//add month datepicker
$("#datepicker").datepicker( {
    format: "mm-yyyy",
    viewMode: "months", 
    minViewMode: "months"
});


