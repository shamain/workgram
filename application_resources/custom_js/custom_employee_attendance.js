//add year datepicker
$("#att_year_dpicker").datepicker( {
    format: "mm-yyyy",
    viewMode: "year", 
    minViewMode: "year"
});

//add month datepicker
$("#att_month_dpicker").datepicker( {
    format: "mm-yyyy",
    viewMode: "months", 
    minViewMode: "months"
});


